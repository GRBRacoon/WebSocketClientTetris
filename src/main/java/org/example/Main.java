package org.example;

import org.json.simple.JSONObject;

import java.net.URI;

public class Main {


    public static void main(String[] args) {
        String serverUri = "http://localhost:8080/";
        String socketUri = "ws://localhost:8080/ws/chat";
        //MyWebSocketClient client=new MyWebSocketClient(URI.create(socketUri));

        //WebSocketService socketService= new WebSocketService(serverUri,socketUri);

        //socketService.CreateRoom();
        //socketService.joinRoom();
        //socketService.sendMessage();
        //socketService.exitRoom();

    }

}